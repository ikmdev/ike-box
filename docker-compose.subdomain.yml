# docker-compose.subdomain.yml
version: '3.8'

services:
  reverse-proxy:
    image: nginx
    ports:
      - "${NGINX_PORT:-8080}:80"
    volumes:
      - ./nginx/subdomain.conf:/etc/nginx/nginx.conf
    networks:
      - web

networks:
  web:
    driver: bridge
